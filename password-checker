#include <iostream>
using namespace std;

int main() {
    string correctPassword = "1234";
    string userInput;

    cout << "Enter password: ";
    cin >> userInput;

    if(userInput == correctPassword) {
        cout << "Access Granted";
    } else {
        cout << "Access Denied";
    }

    return 0;
}
